#include "main.h"
/**
 * 10-add - Check description
 * Check description: adds two integers
 * Return: addition of x and y
 */
int add(int x, int y);
{
	int n;

	n = x + y;
	_putchar(n);

	return (0);
}
